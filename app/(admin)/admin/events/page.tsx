import { EventsDataTable } from '@/components/admin/events/EventsDataTable';

export const metadata = {
  title: 'Événements — Admin AVA',
};

export default function AdminEventsPage() {
  return <EventsDataTable />;
}
